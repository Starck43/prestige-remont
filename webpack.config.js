
export default {
   module: {
    rules: [
      {
/*
        test: /\.(woff2|woff|eot|ttf|otf)$/,
        use: {
         // loader: "url-loader?limit=10000&mimetype=application/font-woff",
          loader: 'file-loader',
          options: {
            outputPath: "fonts/",
            name: "[name].[ext]",
          },
        },
*/
      },
    ],
  },
}
